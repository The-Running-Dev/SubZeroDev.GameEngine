{
  "name": "game-engine",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Deterministic narrative game engine — core + kinds. Specs: ../../docs/docs/engine/.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "eslint": "^9.9.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.5.0",
    "vitest": "^2.1.0"
  }
}
