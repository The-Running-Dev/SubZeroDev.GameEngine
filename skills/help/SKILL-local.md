## vocabulary
`AGENTS.md` is named `CLAUDE.md` here. Slice ids use the retained `W<n>` scheme, not `S<n>` —
read `/slice S3` below as `/slice W<n>`.

## document-map
`design/` here is a compound canonical set with generated `human-doc` blocks; file existence
alone is a weaker stage signal than in a bare repository. Read the marked delivery ledger in
`design/30-slices.md` rather than inferring status from the generated `docs/docs/engine/TODO.md`.
