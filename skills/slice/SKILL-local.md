## vocabulary
`AGENTS.md` is named `CLAUDE.md` here. Slice ids use the retained `W<n>` scheme, not `S<n>` —
`slice/S<n>`, `S3.1`, and the issue-title match below all read with `W<n>` in place of `S<n>`.

## document-map
`design/` here is a compound canonical set with generated `human-doc` blocks; read the marked
delivery ledger in `design/30-slices.md` in full, not the generated `docs/docs/engine/TODO.md`
copy.
